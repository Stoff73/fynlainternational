---
type: handover
mode: context-clear
date: 2026-05-08
session: 7
branch: refactor/uk-pack-relocation
previous_session: 2026-05-08-session-6-clear
---

# Context Clear Handover — 2026-05-08, Session 7

## Immediate state

R-14 SHIPPED — 1 commit pushed (`6da4cf1`). Branch tip at `6da4cf1`, **63 commits ahead of `main`**, working tree clean. Pest **2,824 passing** / 1 skipped / 1 flake. Architecture **130 passing** (was 129; +1 new `tests/Architecture/PackRoutesTest.php`).

## The thread

- Session opened from `handover-2026-05-08-session-6-clear.md` recommending **R-14 routing realignment (~3 hr)** per `architecture-plan-v3.md` § 16. The handover flagged a 308 vs 301 footgun for non-GET methods (POST body loss in some HTTP clients) and asked the next session to add a `LegacyUkApiRedirect` middleware.
- **First investigation found a pre-existing solution.** `core/app/Core/Http/Middleware/LegacyApiRewrite.php` already existed with full test coverage (`tests/Unit/Core/Http/Middleware/LegacyApiRewriteTest.php`, 6 it-blocks including POST body preservation). The Kernel comment in the api group explicitly said "Wire it only when the /api/gb/* routes land (Workstream 0.6+)" — i.e. now. The middleware does in-process URL rewrite via `REQUEST_URI` mutation + `Request::initialize()`, NOT an HTTP redirect. POST/PUT/PATCH/DELETE bodies pass through unchanged. The 301/308 footgun the handover flagged was sidestepped entirely by reusing this in-process rewriter rather than building a redirect layer.
- **Plan revised to 4 narrow changes, all minimal:**
  1. `packs/country-gb/src/Providers/GbPackServiceProvider.php` — `prefix('api')` → `prefix('api/gb')` (one line).
  2. `core/app/Core/Http/Middleware/LegacyApiRewrite.php` — `REWRITABLE_PREFIXES` trimmed 27→17 to only the prefixes whose controllers are actually pack-resident today. Comment rewritten. Other UK-specific modules (`dashboard`, `goals`, `property`, `household`, `journey`, `life-events`, `life-stage`, `family-members`, `business-interests`, `chattels`, `cash-accounts`, `personal-accounts`, `profile-completeness`, `mortgages`, `properties`, `net-worth`, `onboarding`, `holistic-planning` typo, `income-definitions`) are dropped — keeping them in the list would 404 otherwise-working core routes once the rewrite kicks in. The list grows back as those controllers relocate in R-14b.
  3. `app/Http/Kernel.php` — `LegacyApiRewrite` added to **global `$middleware`** (between `ConvertEmptyStringsToNull` and `SecurityHeaders`). NOT to `$middlewareGroups['api']` — the group runs after route matching, by which time rewriting `REQUEST_URI` is too late. Global middleware runs in `HttpKernel::sendRequestThroughRouter()` BEFORE `$router->dispatch($request)`. Obsolete "NOT wired here" comment in the api group rewritten.
  4. `tests/Unit/Core/Http/Middleware/LegacyApiRewriteTest.php` — prefix-list test updated to match the trimmed 17 entries; new test asserts core-resident UK modules are NOT rewritten (proves the trim works as intended). 6 it-blocks → 7 it-blocks.
- **Frontend forward-looking task:** Added a dormant `/za/:pathMatch(.*)*` catch-all redirect in `resources/js/router/index.js` after the existing 5 specific `/za/*` routes. Vue Router 4 first-match ordering means the specific routes still win; catch-all only fires when those specific routes get extracted into the SA pack's `routes.js` (a later workstream — R-13b only relocated SA components). Redirect is robust against both array and string `pathMatch` shapes.
- **Architecture test:** New `tests/Architecture/PackRoutesTest.php` asserts no `/za/` URL literal appears in `packs/country-za/resources/js/routes.js`. Soft-passes today because the file doesn't exist (R-13b only relocated components, not routes); becomes the active guard the moment that file is created.
- **Verification:**
  - Architecture suite: 129 → 130 (+1 new arch test). All green.
  - `tests/Feature/Protection/` (25 tests, 66 assertions) all pass — feature tests hit legacy `/api/protection/*` URLs which are rewritten through the global middleware. End-to-end proof the wiring works in the real HTTP pipeline (not just unit-test isolation).
  - Full Pest suite: 2,824 passing / 1 skipped / 1 failing. The 1 failure is `Tests\Unit\Agents\SavingsAgentGoalsTest > recommends increasing contributions for behind-schedule savings goal` — confirmed flaky: passes 3/3 in isolation at 26-71s variance, fails ~50% of the time in full suite. Confirmed unrelated to R-14 by the consistency of timing-related failures and zero overlap with HTTP layer. **Does NOT block R-14**, but should be tagged for separate investigation (likely a clock-dependent assertion in the agent's behind-schedule recommendation logic).
  - **Browser-tested end-to-end** at `http://localhost:8001`: Logged in as `john@example.com` → `/dashboard`. Captured 22 dashboard API calls on legacy `/api/{protection,savings,investment,retirement,estate,plans,tax-year}/*` URLs (incl. POST `/api/protection/analyze`, POST `/api/retirement/analyze`, POST `/api/estate/calculate-iht`) — all 200 OK. Live `local.INFO: LegacyApiRewrite: rewriting URL …` entries in `storage/logs/laravel.log` confirm the middleware logs each rewrite with `from`, `to`, `method`, `user_agent`, `user_id` fields — gives client-deprecation visibility for the 60-day window per ADR-004.
- **Three diagnostic detours worth flagging for next session:**
  1. **Two PHP servers running on different ports.** Port 8000 is the OLD UK-only `fynla` repo (`/Users/CSJ/Desktop/fynla`), port 8001 is `fynlaInternational` (this repo). Initial Playwright session hit 8000 by mistake — the verification code never showed up because the wrong DB was being queried. Always confirm the running server's CWD via `ps aux | grep artisan` before browser testing.
  2. **Stale `public/hot` file pointed Vite to port 5174** but actual Vite is on 5173. Fixed by `echo -n "http://127.0.0.1:5173" > public/hot`. Symptom was MIME type errors and blank dashboard. Per CLAUDE.md troubleshooting table this is documented but the fix is `rm public/hot` for the *server* case (where Vite isn't running). For local dev where Vite IS running, write the correct URL.
  3. **Pest `RefreshDatabase` truncates between tests** — running the full Pest suite in parallel with browser testing wiped the seeded users mid-flow. Don't do that. Either browser-test BEFORE running full Pest, or re-seed afterwards (`php artisan migrate --force && php artisan db:seed --force`).
- **Vault sync (Haiku 4.5 subagent at high effort, vault root `/Users/CSJ/Desktop/fynlaInter/FynlaInter/`)** ran cleanly: `Git History/May08.md` extended (16 commits → 17, refactor type 11 → 12), `May2026 Commits.md` totals updated (78 → 79; refactor 45 → 46; branch tip → `6da4cf1`; ahead-of-main → 63), `May Index.md` session 7 entry added with full R-14 narrative, `Home.md` Git History May 2026 row updated and Key Decisions section narrative bumped to "R-0 → R-14 SHIPPED" with remaining hours adjusted (15 → 14). 0 broken wikilinks, 0 orphans, all frontmatter compliant. Memory dir audit: 5 files indexed in MEMORY.md, all current, 0 stale, 0 contradictions. The subagent considered whether the in-process REQUEST_URI rewrite pattern warranted a new memory entry but concluded it's already captured in the R-14 commit message and `May08.md` narrative — no separate memory needed.

## Files touched (all committed + pushed)

**R-14 commit (`6da4cf1`):** 6 files, +116 / -36
- `packs/country-gb/src/Providers/GbPackServiceProvider.php` — `prefix('api')` → `prefix('api/gb')`; comment rewritten to reference the global LegacyApiRewrite.
- `core/app/Core/Http/Middleware/LegacyApiRewrite.php` — `REWRITABLE_PREFIXES` trimmed to 17 entries; comment rewritten to explain the "only pack-relocated prefixes" invariant.
- `app/Http/Kernel.php` — `Fynla\Core\Http\Middleware\LegacyApiRewrite::class` added to global `$middleware` between `ConvertEmptyStringsToNull` and `SecurityHeaders`; api-group "NOT wired here" comment rewritten.
- `resources/js/router/index.js` — `/za/:pathMatch(.*)*` dormant catch-all redirect inserted after `/za/protection` route. Robust against array or string `pathMatch`. Preserves query + hash.
- `tests/Unit/Core/Http/Middleware/LegacyApiRewriteTest.php` — prefix-list test updated to mirror the trimmed 17; new test "does not rewrite UK module prefixes still resident in core routes" asserts the 17 core-resident prefixes pass through unchanged.
- `tests/Architecture/PackRoutesTest.php` — NEW. 1 it-block: `'SA pack routes file declares no /za/ URL prefix'`. Soft-passes when `packs/country-za/resources/js/routes.js` doesn't exist; activates as a guard once that file is created.

Working tree at handover write-time: clean except for this handover file + CSJTODO update, which session-end Phase 10 will commit + push as one final docs commit.

## What the next Claude needs to know

1. **Branch state:** `refactor/uk-pack-relocation` at `6da4cf1` (after Phase 10 will be `6da4cf1` + 1 docs commit), **63 → 64 commits ahead of `main`**, all pushed. Pest **2,824 passing** / 1 skipped / 1 flake. Architecture **130 passing**.
2. **R-14 is CLOSED.** Next workstream is **R-14a — ADR-005 int-minor money refactor + relocation of deferred float-money services (~6 hr — provisional)** per `architecture-plan-v3.md` § 16a. Re-scope at kickoff because the deferral list grew during R-6 → R-9. Current targets per the plan: 2 traits (`FormatsCurrency`, `CalculatesOCF`), 7 Estate services (`IHTFormattingService`, `PersonalizedGiftingStrategyService`, `PersonalizedTrustStrategyService`, `TrustService`, `GiftingStrategy`, `IntestacyCalculator`, `FutureValueCalculator`), 5 Tax services (`IncomeDefinitionsService`, `TaxOptimisationService`, `TaxActionDefinitionService`, `TaxBandTracker`, `UKTaxCalculator`), plus whatever R-6/R-7/R-8/R-9 added with R-15 ratchet markers. The full deferral list is in `tests/Architecture/PackIsolationTest.php` allow-list (~59 entries at last count).
3. **Don't pre-empt R-14b.** The 6 deferred core models (`User`, `Household`, `Goal`, `GoalContribution`, `LifeEvent`, `LifeEventAllocation`) and the UK controllers still in `app/Http/Controllers/Api/` (dashboard, goals, property, mortgages, household, journey, life-events, business-interests, chattels, cash-accounts, personal-accounts, profile-completeness, family-members, etc.) all gate on R-14b's container query layer. Don't move them as part of R-14a — R-14a is float-money refactor only.
4. **`LegacyApiRewrite::REWRITABLE_PREFIXES` will need additions during R-14b.** When R-14b moves `dashboard`/`goals`/`property` etc. controllers into `packs/country-gb/routes/api.php`, those prefixes need adding back to the rewrite list AND removing from the negative test (`'does not rewrite UK module prefixes still resident in core routes'`). The current negative test serves as a regression marker — when a prefix moves, both lists update.
5. **The 1 flaky test is `SavingsAgentGoalsTest > recommends increasing contributions for behind-schedule savings goal`** at `tests/Unit/Agents/SavingsAgentGoalsTest.php:54`. Confirmed unrelated to R-14 (passes deterministically in isolation). Likely cause: clock-dependent assertion — the test sets `target_date: now()->addMonths(6)` and the agent computes a "behind schedule" recommendation based on time-to-target; if the test runs on a non-deterministic clock or the agent's threshold lands too close to today's date, the recommendation drops. Recommend a separate ~30 min investigation BEFORE R-14a begins so the test suite returns to a clean 2,824 passing / 0 failing baseline. Quick fix probably involves freezing `now()` with `Carbon::setTestNow()` in the test's `beforeEach()`.
6. **Two-server gotcha** — port 8000 is the legacy `/Users/CSJ/Desktop/fynla` UK-only repo's PHP server, port 8001 is `fynlaInternational`. Always `ps aux | grep artisan` before browser testing. Vite is on 5173 for fynlaInternational; if `public/hot` is wrong (says 5174 or any other port), `echo -n "http://127.0.0.1:5173" > public/hot` fixes the MIME-type-error blank dashboard.
7. **Vault path is `/Users/CSJ/Desktop/fynlaInter/FynlaInter/`, NOT `fynlaBrain/`.** Restated for emphasis (fynlaBrain mirror also exists up to session 6 but is historical — sync goes to fynlaInter only). The session-end skill text still hard-codes `fynlaBrain` but the dispatch prompt overrides the path explicitly.
8. **CLAUDE.md metric drift continues, do not auto-fix.** Vault sync's Phase 1 reports the count is roughly current except for inflated services/models due to packs being counted (expected, R-15 re-baselines). Vue 713, Controllers 99, Vuex 39, Agents 9 still match.
9. **No SA preview personas exist.** R-13b's browser test was limited to "SPA bundle loads + route guard returns 404 cleanly". A full SA journey verification needs a SA-active preview persona. R-14 didn't change this — still a separate workstream not in the v3 plan budget. Won't block R-14a or R-14b.
10. **R-14a deferral count: ~59 entries.** R-14 added zero new entries — pure HTTP routing work doesn't touch service signatures. Architecture suite stayed at 129 → 130 (the +1 is the new `PackRoutesTest`). The R-14a kickoff task is to enumerate the current allow-list, group by trait/service, and decide a sub-batching strategy (Estate first? Tax first? both together?). The plan's 6-hour estimate is provisional — re-budget after the enumeration.

## Pick up from here

**Recommendation: R-14a — ADR-005 int-minor money refactor + relocation (~6 hr — provisional, re-scope at kickoff).** Approach:

1. **Re-scope first.** Open `Plans/architecture-plan-v3.md` § 16a. Run `git grep -nE "/\* R-14a|R-15 ratchet|float \\\$amount" tests/Architecture/PackIsolationTest.php` to enumerate the current deferral list. Group by domain (Traits / Estate / Tax / new-from-R-9). Confirm the 6-hour estimate is still in the right ballpark.
2. **Optional pre-task: fix the SavingsAgentGoalsTest flake.** ~30 min. Add `Carbon::setTestNow(now())` to the `beforeEach()` block in `tests/Unit/Agents/SavingsAgentGoalsTest.php` so all three tests in the file run against a frozen clock. Run the test 5× to confirm it's deterministic. Commit as `test: fix SavingsAgentGoalsTest clock-dependent flake`. This brings the suite back to 2,824 passing / 0 failing before R-14a starts touching domain logic.
3. **Per-file R-14a procedure** (one commit per logical batch — Estate, then Tax, then traits):
   - For each file: convert every `float $amount`-style parameter and return type to `int $amountMinor` (pence). Convert internal arithmetic to integer math (or BCMath where decimal precision matters — e.g. percentage applied on a pension contribution).
   - Update every caller (services, agents, controllers, observers, tests) to pass and receive minor units. Use `(int) round($amount * 100)` only at boundaries (form-request validation, view formatting).
   - Update factories + seeders that generate fixture amounts.
   - Move the file into `packs/country-gb/src/{Estate,Tax,Traits}/` with the standard relocation procedure (namespace update, `use` statements in callers, sed of bare class refs).
   - Remove the file's allow-list entry from `tests/Architecture/PackIsolationTest.php`.
4. **Bindings:** Already-bound services (`pack.gb.tax`, `pack.gb.estate`, etc.) get their `bind()` targets repointed from `\App\…` to `\Fynla\Packs\Gb\…` as each file moves. Some services aren't bound — they're called through other services that ARE bound; for these, the bare-class-reference fix from R-3/R-5 pattern applies.
5. **Verification per batch:**
   - `./vendor/bin/pest --testsuite=Architecture` — green, including `NoFloatMoneyTest` newly active inside `packs/country-gb/`.
   - `./vendor/bin/pest` — full suite green (2,824 + any new tests added during R-6 → R-13 must still pass).
   - `tests/Architecture/PackIsolationTest.php` allow-list shrinks by the number of files moved.
   - Spot-check one Estate calculation end-to-end at the API level to confirm minor-unit arithmetic produces the same display value as before.
6. **Risks to watch:**
   - **Loss of precision around percentage applies.** The most subtle bugs are in `$balance * $rate` where `$rate` is a float fraction. Converting `$balance` to pence-as-int while leaving `$rate` as float is fine, but the rounding mode at result-conversion (back to display pounds) must be consistent with the existing UI expectation.
   - **Test fixture drift.** Factories that produced floats now produce ints — any test that asserts an exact pound value needs to be re-checked. The arch test will surface signature mismatches; the Pest suite will surface assertion drift.
   - **Mid-workstream blow-out.** If the deferral list has grown beyond ~30 entries by the time R-14a starts, the 6-hour estimate is wrong. Re-scope at kickoff before starting moves.
7. Commit + push per logical batch (e.g. `refactor(uk-pack): R-14a-Estate — int-minor money refactor + relocation of 7 Estate services`). After all batches: `refactor(uk-pack): R-14a closed — N services + 2 traits relocated, allow-list shrunk by N entries`.

**Alternative if R-14a feels too entangled with concurrent flake-fixing or domain-precision concerns:** jump to **R-14b (~5 hr — provisional)** — container-resolved query layer + relocation of 6 deferred core models (`User`, `Household`, `Goal`, `GoalContribution`, `LifeEvent`, `LifeEventAllocation`). R-14b is bigger architecturally but more mechanical (no float-to-int conversion). Pick R-14a first if you want to maintain symmetry with the v3 plan's stated order; pick R-14b first if money-precision concerns make R-14a feel risky.

**DO NOT** introduce compatibility shims, aliases, or fallback layers. The whole v3 plan is "direct relocation, no compat aliases" — the antipattern that broke the April attempt. R-14's `LegacyApiRewrite` is an EXPLICIT one-shot legacy bridge with a 60-day removal window per ADR-004 § 8.3, not a permanent compat surface. Don't reintroduce the antipattern.
