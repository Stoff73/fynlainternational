---
name: LH8000 shorthand
description: User uses LH8000 to mean localhost:8000 — implement changes on the live dev server
type: feedback
---

LH8000 means localhost:8000. When the user says "make changes on LH8000" they want changes implemented in the actual codebase so they appear on the dev server.

**Why:** Shorthand the user prefers for referencing the local development server.
**How to apply:** Treat "LH8000" as "implement on localhost:8000" — make actual code changes, not just mockup updates.
