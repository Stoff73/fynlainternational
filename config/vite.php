<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Build Path
    |--------------------------------------------------------------------------
    |
    | The build path is the directory where Vite will output the compiled
    | assets. For ROOT directory deployment, this should be 'build' instead
    | of the default 'build' under public_path (which would be public/build).
    |
    */

    'build_path' => env('VITE_BUILD_PATH', 'build'),

];
